{
  "_comment": "Merge the permissions block into ~/.claude/settings.json (keep whatever keys already exist there). Deny rules at user level win over any project allow rule. Bare filenames match at any depth (gitignore semantics), so Read(.env) == Read(**/.env). disableBypassPermissionsMode self-locks bypass mode out — remove the key if you ever genuinely need it.",
  "permissions": {
    "deny": [
      "Read(**/.env)",
      "Read(**/.env.*)",
      "Read(**/*.pem)",
      "Read(**/*.key)",
      "Read(**/id_rsa*)",
      "Read(~/.ssh/**)",
      "Read(~/.aws/**)",
      "Bash(git commit:*)",
      "Bash(git push:*)"
    ],
    "disableBypassPermissionsMode": "disable"
  }
}
