predicate: zizmor .github/workflows/ >/dev/null 2>&1
born: 2026-07-14
source: leveled from all three live projects — workflows must stay at zero zizmor findings (SHA pins, persist-credentials false, least privilege)
status: satisfied
last-pass: never
on-violation: a workflow edit regressed CI security; run `zizmor .github/workflows/` and fix findings, never suppress them
